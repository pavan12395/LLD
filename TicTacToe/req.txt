Readability
Extensibility --> (Add an other game)
Correctness


Features :-

1) GameState should indicate an enum ( PENDING , TIED , OVER) --> (done)
2) If there is no way a user can win , then we need to indicate that the gameState is tied.
2) if the gameState is not pending then and If made a move we need to throw an error to the user



Board --> set and get of the cells

RuleEngine --> Checks all types of conditions on a board

        Conditions :- Only the conditions
            BoardState --> previousPlayer , occupied or not shouldnt be interfered.

GameEngine --> Manages the Board and Moves

AIEngine --> Should have the ability to getSmartMove and getRandomMove extending Entity.




